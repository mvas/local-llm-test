[project]
name = "local-llm-benchmarks"
version = "0.1.0"
description = "Local LLM benchmark runner"
requires-python = ">=3.12"
dependencies = [
    "evalplus>=0.3.1",
    "lm-eval[api,hf,ifeval]>=0.4.11",
]
